<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
        <title>Anagrams</title>
    </head>
    <body>
        <div class="container">
            <h1>Anagrams!</h1>
            <h3><em>Because comparing words is fun?</em></h3>

            <form action="/return_results">
                <div class="form-groups">
                    <label for="master">Enter the master word: </label>
                    <input type="text" name="master" id="master">
                </div>
                <div class="form-groups">
                    <label for="test">Enter a word to check to see if its in master word: </label>
                    <input type="text" name="test" id="test">
                </div>
                <button class="btn btn-success" type="submit">Test for anagrams</button>
            </form><br>

            {% if result %}
                <div class="well">
                    <h1>Your result is true!</h1>
                </div>
            {% else %}
                <div class="well">
                    <h1>Your result is false!</h1>
                </div>
            {% endif %}

        </div>
    </body>
</html>
